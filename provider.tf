# Provider stubs — callers configure these via environment variables or variable passing.
# Do NOT set `token` here; callers should set HCLOUD_TOKEN env var or pass it explicitly.

provider "hcloud" {
  # token set via HCLOUD_TOKEN env var or variable passed from calling module
}
